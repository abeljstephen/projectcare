# Security Report — ProjectCare
**Generated**: 2026-03-28 01:55:47  
**Scope**: static  
**Static findings**: 2 CRITICAL · 422 HIGH · 0 MEDIUM · 0 LOW/INFO  

---

## Static Security Findings

### CRITICAL (2)

- `agents/api-monitor-agent/check-usage.py:59` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `agents/security-monitor-agent/security-agent.py:1078` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

### HIGH (422)

- `agents/patent-protect-agent/patent-agent.py:79` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:84` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:89` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:94` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:99` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:104` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:109` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:114` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:159` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:161` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:163` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:165` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:167` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:169` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:207` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:248` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:250` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:252` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:254` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:256` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:258` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:260` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:265` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:306` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:307` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:346` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:348` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:350` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:352` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:354` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:356` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:358` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:360` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:362` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:400` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:402` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:404` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:406` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:408` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:410` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:412` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/patent-protect-agent/patent-agent.py:467` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:83` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:85` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:87` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:89` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:91` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:289` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: eval() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:289` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: exec() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:293` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: eval() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:293` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:294` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:299` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: eval() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:299` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:301` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: exec() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:301` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:302` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:337` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:338` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:377` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:408` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:446` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:447` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:448` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:481` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:482` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:534` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:570` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:574` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:609` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:610` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:633` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:656` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:657` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:690` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:691` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:692` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:801` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:838` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: compile() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/security-monitor-agent/security-agent.py:943` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: eval() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `agents/gas-archive/archive-versions.py:73` [SEC-011]  ·  OWASP: A03 Injection
  — subprocess call with shell=True — command injection risk.
  **Fix:** Pass command as a list and remove shell=True.

- `agents/security-monitor-agent/security-agent.py:335` [SEC-011]  ·  OWASP: A03 Injection
  — subprocess call with shell=True — command injection risk.
  **Fix:** Pass command as a list and remove shell=True.

- `agents/security-monitor-agent/security-agent.py:355` [SEC-011]  ·  OWASP: A03 Injection
  — subprocess call with shell=True — command injection risk.
  **Fix:** Pass command as a list and remove shell=True.

- `agents/security-monitor-agent/security-agent.py:357` [SEC-011]  ·  OWASP: A03 Injection
  — subprocess call with shell=True — command injection risk.
  **Fix:** Pass command as a list and remove shell=True.

- `agents/security-monitor-agent/security-agent.py:364` [SEC-011]  ·  OWASP: A03 Injection
  — os.system() / os.popen() — command injection risk.
  **Fix:** Replace with subprocess.run(cmd_list, shell=False).

- `agents/security-monitor-agent/security-agent.py:906` [SEC-011]  ·  OWASP: A03 Injection
  — subprocess call with shell=True — command injection risk.
  **Fix:** Pass command as a list and remove shell=True.

- `api/_archive/Code.V10_WORKS.gs:61` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL constructed from variable — SSRF risk.
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.

- `api/_archive/Code.V3_WORKS.gs:61` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL constructed from variable — SSRF risk.
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.

- `api/_archive/Code.V4_WORKS.gs:66` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL constructed from variable — SSRF risk.
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.

- `api/_archive/Code.V5_WORKS.gs:66` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL constructed from variable — SSRF risk.
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.

- `api/_archive/Code.V6_WORKS.gs:66` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL constructed from variable — SSRF risk.
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.

- `api/_archive/Code.V7_WORKS.gs:66` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL constructed from variable — SSRF risk.
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.

- `api/_archive/Code.V8_WORKS.gs:66` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL constructed from variable — SSRF risk.
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.

- `api/_archive/Code.V9_WORKS.gs:61` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL constructed from variable — SSRF risk.
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.

- `api/_archive/Code.gs:61` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL constructed from variable — SSRF risk.
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.

- `api/webapp.gs:488` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL constructed from variable — SSRF risk.
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.

- `agents/security-monitor-agent/security-agent.py:446` [SEC-014]  ·  OWASP: A08 Software Integrity Failures
  — Unsafe deserialization: pickle.load(s) — RCE if input is attacker-controlled.
  **Fix:** Replace pickle with json; use yaml.safe_load() instead of yaml.load().

- `agents/security-monitor-agent/security-agent.py:448` [SEC-014]  ·  OWASP: A08 Software Integrity Failures
  — Unsafe deserialization: marshal.load(s) — RCE if input is attacker-controlled.
  **Fix:** Replace pickle with json; use yaml.safe_load() instead of yaml.load().

- `agents/security-monitor-agent/security-agent.py:469` [SEC-014]  ·  OWASP: A08 Software Integrity Failures
  — Unsafe deserialization: yaml.load() without SafeLoader — RCE if input is attacker-controlled.
  **Fix:** Replace pickle with json; use yaml.safe_load() instead of yaml.load().

- `api/_archive/Plot.V10_WORKS.html:805` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:836` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:885` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:889` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:896` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1209` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1216` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1220` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1306` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1309` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1372` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1375` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1393` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1508` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1516` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1631` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1640` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1778` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1865` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V10_WORKS.html:1894` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V3_WORKS.html:1039` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V3_WORKS.html:1057` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V3_WORKS.html:1058` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V3_WORKS.html:1621` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:553` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:608` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:611` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:617` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:902` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:905` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:968` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:971` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:1005` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:1072` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:1082` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:1083` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:1184` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:1185` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:1194` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:1201` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V4_WORKS.html:1360` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:615` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:670` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:673` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:679` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:990` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:993` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:1056` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:1059` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:1093` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:1206` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:1214` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:1327` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:1336` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:1343` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V5_WORKS.html:1506` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:638` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:693` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:696` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:702` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:1014` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:1017` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:1080` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:1083` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:1117` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:1230` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:1238` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:1351` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:1360` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:1367` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V6_WORKS.html:1530` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:718` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:773` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:776` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:782` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1094` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1097` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1160` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1163` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1197` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1310` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1318` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1431` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1440` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1444` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1451` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1455` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1527` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1596` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V7_WORKS.html:1687` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:811` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:875` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:878` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:884` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1205` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1208` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1271` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1274` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1308` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1420` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1428` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1540` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1549` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1553` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1560` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1564` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1635` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1701` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V8_WORKS.html:1791` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:811` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:875` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:878` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:884` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1178` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1181` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1244` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1247` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1281` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1393` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1401` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1513` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1522` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1526` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1533` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1537` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1608` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1674` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.V9_WORKS.html:1745` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:811` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:875` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:878` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:884` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1178` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1181` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1244` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1247` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1281` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1393` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1401` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1513` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1522` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1526` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1533` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1537` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1608` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1674` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/_archive/Plot.html:1745` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:1921` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:3001` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:3019` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:3135` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:3136` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4216` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4247` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4249` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4253` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4374` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4375` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4376` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4425` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4426` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4427` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4750` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4975` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4976` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:4977` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:5007` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:5010` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:5030` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:5033` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:5049` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:6172` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:7206` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:7211` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:7214` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:7371` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:7455` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:7477` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:7488` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:7517` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:7529` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:7557` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:7574` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:8322` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:9347` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:9353` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:9380` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:9393` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:9410` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:9999` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:11815` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:11858` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:11889` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:11902` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:12332` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:12349` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:12410` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:12456` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:12639` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/addon/Plot.html:12657` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `api/tests/plot-preview.js:150` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `engines/saco-browser/test.html:413` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `engines/saco-browser/test.html:447` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `integrations/wordpress/pmc-estimator/assets/js/app.js:313` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `integrations/wordpress/pmc-estimator/assets/js/app.js:317` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `integrations/wordpress/pmc-estimator/assets/js/app.js:943` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `integrations/wordpress/pmc-estimator/assets/js/app.js:998` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `integrations/wordpress/pmc-estimator/assets/js/app.js:1001` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `integrations/wordpress/pmc-estimator/assets/js/app.js:1102` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `integrations/wordpress/pmc-estimator/assets/js/app.js:1496` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `integrations/wordpress/pmc-estimator/assets/js/app.js:1517` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `integrations/wordpress/pmc-estimator/assets/js/app.js:1816` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `integrations/wordpress/pmc-estimator/assets/js/app.js:1857` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:2110` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:3190` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:3208` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:3324` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:3325` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:4405` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:4436` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:4438` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:4442` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:4564` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:4565` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:4566` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:4615` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:4616` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:4617` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:4940` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:5165` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:5166` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:5167` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:5197` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:5200` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:5220` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:5223` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:5239` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:6362` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:7446` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:7451` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:7454` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:7611` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:7695` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:7717` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:7728` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:7757` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:7769` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:7797` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:7814` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:8833` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:9858` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:9864` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:9891` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:9904` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:9921` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:10510` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:12298` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:12341` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:12372` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:12385` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:12815` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:12832` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:12893` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:12939` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:13122` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/index.html:13140` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/plot.js:350` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/plot.js:454` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/plot.js:461` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/plot.js:519` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/plot.js:543` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/plot.js:548` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `plot/plot.js:772` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `integrations/wordpress/wp-config.php` [SEC-030]  ·  OWASP: A05 Security Misconfiguration
  — PHP file lacks ABSPATH guard — can be executed directly.
  **Fix:** Add: if ( ! defined( 'ABSPATH' ) ) exit; at the top of the file.

- `integrations/wordpress/projectcare-crm/includes/admin/activity-log.php:131` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/activity-log.php:156` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/bulk-email.php:68` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/bulk-email.php:83` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/bulk-email.php:92` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/bulk-email.php:93` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/bulk-email.php:126` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/dashboard.php:129` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/dashboard.php:158` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/email-templates.php:91` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/email-templates.php:92` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/email-templates.php:112` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/email-templates.php:124` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/gas-status.php:69` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/gas-status.php:119` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/gas-status.php:134` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/gas-status.php:135` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/gas-status.php:136` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/gas-status.php:215` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/plans-editor.php:92` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/plans-editor.php:93` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/promo-codes.php:136` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/promo-codes.php:137` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/promo-codes.php:143` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/settings.php:246` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/stripe-log.php:111` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/tools.php:234` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/user-detail.php:186` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/user-detail.php:187` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/user-detail.php:378` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/users-list.php:390` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/users-list.php:391` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/users-list.php:393` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/users-list.php:420` [SEC-031]  ·  OWASP: A03 Injection
  — PHP echo/print with variable not wrapped in escape function.
  **Fix:** Wrap with esc_html(), esc_attr(), or esc_url() as appropriate.

- `integrations/wordpress/projectcare-crm/includes/admin/activity-log.php` [SEC-032]  ·  OWASP: A01 Broken Access Control
  — PHP file reads $_POST/$_GET but no nonce verification found.
  **Fix:** Add wp_verify_nonce() or check_admin_referer() before processing input.

- `integrations/wordpress/projectcare-crm/includes/admin/activity-log.php:14` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_GET access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/activity-log.php:24` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_GET access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/dashboard.php:76` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_GET access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/email-templates.php:30` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_POST access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/promo-codes.php:24` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_POST access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/settings.php:46` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_POST access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/stripe-log.php` [SEC-032]  ·  OWASP: A01 Broken Access Control
  — PHP file reads $_POST/$_GET but no nonce verification found.
  **Fix:** Add wp_verify_nonce() or check_admin_referer() before processing input.

- `integrations/wordpress/projectcare-crm/includes/admin/stripe-log.php:15` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_GET access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/tools.php:10` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_GET access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/tools.php:111` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_POST access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/user-detail.php:31` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_POST access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/users-list.php:6` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_GET access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/users-list.php:18` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_GET access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.

- `integrations/wordpress/projectcare-crm/includes/admin/users-list.php:117` [SEC-033]  ·  OWASP: A03 Injection
  — Unsanitised $_GET access.
  **Fix:** Wrap with sanitize_text_field(), intval(), or esc_*() before use.
