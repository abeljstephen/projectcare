# Security Report — ProjectCare
**Generated**: 2026-03-14 09:02:58  
**Scope**: static  
**Static findings**: 16 CRITICAL · 139 HIGH · 10 MEDIUM · 3 LOW/INFO  

---

## Static Security Findings

### CRITICAL (16)

- `agents/math-agent/QUICKSTART.md:16` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `agents/math-agent/QUICKSTART.md:22` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `agents/math-agent/QUICKSTART.md:105` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `agents/math-agent/README.md:37` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `agents/qa-agent/QUICKSTART.md:6` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `agents/qa-agent/README.md:42` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `agents/research-agent/QUICKSTART.md:14` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `agents/research-agent/QUICKSTART.md:19` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `agents/research-agent/QUICKSTART.md:113` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `agents/research-agent/README.md:44` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `agents/research-agent/research-agent.py:123` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `system-google-sheets-addon/config/CONFIG.md:117` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `system-google-sheets-addon/config/CONFIG.md:121` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `system-google-sheets-addon/config/CONFIG.md:343` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `system-google-sheets-addon/config/config-api/providers/grok_provider.py:17` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

- `system-google-sheets-addon/docs/architecture/INFRASTRUCTURE_SUMMARY.md:305` [SEC-002]  ·  OWASP: A02 Cryptographic Failures
  — Hardcoded credential pattern 'generic_secret' found.
  **Fix:** Remove from file, rotate the credential, update .gitignore.

### HIGH (139)

- `git-history` [SEC-004]  ·  OWASP: A02 Cryptographic Failures
  — Secret pattern 'generic_secret' found in git history (added line). Key may still be valid even if removed from current HEAD.
  **Fix:** Rotate the credential immediately. Consider git-filter-repo to purge history.

- `git-history` [SEC-004]  ·  OWASP: A02 Cryptographic Failures
  — Secret pattern 'generic_secret' found in git history (added line). Key may still be valid even if removed from current HEAD.
  **Fix:** Rotate the credential immediately. Consider git-filter-repo to purge history.

- `system-google-sheets-addon/tests/plot-preview.js:300` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: exec() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `system-google-sheets-addon/tests/qa-publish.js:104` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: exec() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `wordpress-plugin/pmc-estimator/assets/js/app.js:1732` [SEC-010]  ·  OWASP: A03 Injection
  — Dynamic code execution: exec() detected.
  **Fix:** Replace with safe alternatives. If unavoidable, validate input against strict allowlist.

- `system-google-cloud-functions-api/Code.gs:129` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL constructed from variable — SSRF risk.
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.

- `system-google-cloud-functions-api/Plot.html:1563` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:1599` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:1648` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:1652` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:1659` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:1977` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:1981` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:1990` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:1994` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2080` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2098` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2101` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2136` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2139` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2169` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2284` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2292` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2407` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2416` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2554` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2641` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/Plot.html:2670` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/core/Plot.html:1977` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/core/Plot.html:2310` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/submit.html:193` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/submit.html:194` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/submit.html:199` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-cloud-functions-api/submit.html:200` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:1792` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:3230` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:3248` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:3364` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:3365` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4442` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4473` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4475` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4479` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4600` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4601` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4602` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4651` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4652` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4653` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4754` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4959` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4960` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4961` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4991` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:4994` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:5014` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:5017` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:5033` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:6156` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:7158` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:7163` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:7166` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:7322` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:7401` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:7413` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:7441` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:7458` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:8081` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:9098` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:9104` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:9131` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:9133` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:9144` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:9152` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:9161` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:9175` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:9750` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:11566` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:11609` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:11640` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:11653` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:12083` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:12100` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:12161` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:12207` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:12390` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/Plot.html:12408` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-google-sheets-addon/tests/plot-preview.js:148` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:1788` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:1794` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:1812` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:1853` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:1854` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:1868` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:1869` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:1875` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:1876` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:2354` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:2388` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:3855` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:3983` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:4329` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:4330` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:4375` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:4382` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:5526` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:5900` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:7125` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:7126` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:7140` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:7141` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:7147` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:7148` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:7597` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:7797` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:7875` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:8099` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:8127` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:8211` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:8259` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:8416` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:8428` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:8476` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:8540` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:8687` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:9318` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/Plot.html:9348` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `system-original-web-app-api/submit.html:172` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `wordpress-plugin/pmc-estimator/assets/js/app.js:313` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `wordpress-plugin/pmc-estimator/assets/js/app.js:317` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `wordpress-plugin/pmc-estimator/assets/js/app.js:943` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `wordpress-plugin/pmc-estimator/assets/js/app.js:998` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `wordpress-plugin/pmc-estimator/assets/js/app.js:1001` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `wordpress-plugin/pmc-estimator/assets/js/app.js:1102` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `wordpress-plugin/pmc-estimator/assets/js/app.js:1496` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `wordpress-plugin/pmc-estimator/assets/js/app.js:1517` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `wordpress-plugin/pmc-estimator/assets/js/app.js:1816` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `wordpress-plugin/pmc-estimator/assets/js/app.js:1857` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

- `wordpress-plugin/pmc-estimator/assets/js/engine/viz3d.js:532` [SEC-020]  ·  OWASP: A03 Injection
  — innerHTML assignment with variable — potential XSS if value contains user input.
  **Fix:** Wrap dynamic values with escHtml() before assigning to innerHTML.

### MEDIUM (10)

- `system-google-cloud-functions-api/Code.gs:106` [SEC-022]  ·  OWASP: A09 Logging / Monitoring Failures
  — Logger/console.log appears to log a sensitive field name.
  **Fix:** Remove or redact sensitive fields before logging.

- `system-google-cloud-functions-api/Code.gs:107` [SEC-022]  ·  OWASP: A09 Logging / Monitoring Failures
  — Logger/console.log appears to log a sensitive field name.
  **Fix:** Remove or redact sensitive fields before logging.

- `system-google-cloud-functions-api/Code.gs:113` [SEC-022]  ·  OWASP: A09 Logging / Monitoring Failures
  — Logger/console.log appears to log a sensitive field name.
  **Fix:** Remove or redact sensitive fields before logging.

- `system-google-cloud-functions-api/Code.gs:115` [SEC-022]  ·  OWASP: A09 Logging / Monitoring Failures
  — Logger/console.log appears to log a sensitive field name.
  **Fix:** Remove or redact sensitive fields before logging.

- `system-google-cloud-functions-api/test_scripts/test_api_simple.js:91` [SEC-022]  ·  OWASP: A09 Logging / Monitoring Failures
  — Logger/console.log appears to log a sensitive field name.
  **Fix:** Remove or redact sensitive fields before logging.

- `system-google-cloud-functions-api/test_scripts/test_decision_sliders.js:250` [SEC-022]  ·  OWASP: A09 Logging / Monitoring Failures
  — Logger/console.log appears to log a sensitive field name.
  **Fix:** Remove or redact sensitive fields before logging.

- `system-google-cloud-functions-api/test_scripts/test_decision_sliders.js:254` [SEC-022]  ·  OWASP: A09 Logging / Monitoring Failures
  — Logger/console.log appears to log a sensitive field name.
  **Fix:** Remove or redact sensitive fields before logging.

- `system-google-cloud-functions-api/test_scripts/test_prod_code_gs.js:202` [SEC-022]  ·  OWASP: A09 Logging / Monitoring Failures
  — Logger/console.log appears to log a sensitive field name.
  **Fix:** Remove or redact sensitive fields before logging.

- `system-google-cloud-functions-api/test_scripts/test_prod_code_gs.js:206` [SEC-022]  ·  OWASP: A09 Logging / Monitoring Failures
  — Logger/console.log appears to log a sensitive field name.
  **Fix:** Remove or redact sensitive fields before logging.

- `system-google-cloud-functions-api/Code.gs:468` [SEC-042]  ·  OWASP: A03 Injection
  — HtmlService output created — verify GAS variables are escaped with HtmlService.htmlEscape() or use <?= auto-escape syntax.
  **Fix:** Use <?= scriptlet ?> in templates (auto-escapes) or htmlEscape() explicitly.

### LOW (2)

- `system-google-sheets-addon/Plot.html` [SEC-023]  ·  OWASP: A05 Security Misconfiguration
  — No Content-Security-Policy meta tag found.
  **Fix:** Add <meta http-equiv="Content-Security-Policy" content="..."> to <head>.

- `wordpress-plugin/pmc-estimator/templates/estimator.html` [SEC-023]  ·  OWASP: A05 Security Misconfiguration
  — No Content-Security-Policy meta tag found.
  **Fix:** Add <meta http-equiv="Content-Security-Policy" content="..."> to <head>.

### INFO (1)

- `system-google-cloud-functions-api/Code.gs:97` [SEC-013]  ·  OWASP: A10 SSRF
  — UrlFetchApp.fetch() — URL appears static (OK).
  **Fix:** Validate URL against a whitelist before passing to UrlFetchApp.
